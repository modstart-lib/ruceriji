/**
 * 视觉设计 Tokens — 全局统一配置入口
 *
 * 修改颜色等视觉属性时，优先改此文件，并同步更新 `token.less` 中的 CSS 变量。
 *
 * 使用场景：
 *   - 图表、Canvas、运行时主题、与 CSS 无关的 TS 逻辑中读取色值
 *   - 与 uview-plus / 组件 theme 配置对接时引用常量
 */

export const designTokens = {
  color: {
    // ── 品牌主色（基准 #111111，中性黑）────────────────────────────────
    /** 亮色模式主色 */
    primary: '#111111',
    /** 暗色模式主色（偏浅，保证深色背景上的对比度） */
    primaryDark: '#e5e5e5',
    /** 主色悬停 / 加深态 */
    primaryHover: '#000000',

    // ── 语义色 ───────────────────────────────────────────────────────
    /** 成功 */
    success: '#52c41a',
    /** 警告 */
    warning: '#faad14',
    /** 错误 / 危险 */
    error: '#ff4d4f',
    /** 强调 */
    accent: '#722ed1',

    // ── 页面背景（亮色）──────────────────────────────────────────────
    /** 页面底层背景 */
    surface: '#ffffff',
    /** 卡片 / 区块背景 */
    panel: '#f9fafb',
    /** 页面灰底（与 body / page 背景一致） */
    body: '#f8f8f8',

    // ── 页面背景（暗色）──────────────────────────────────────────────
    surfaceDark: '#111827',
    panelDark: '#1f2937',

    // ── 正文文字 ─────────────────────────────────────────────────────
    /** 正文（亮色）：默认黑 #111111 */
    text: '#111111',
    textDark: '#f3f4f6',
  },
} as const

export type DesignTokens = typeof designTokens
